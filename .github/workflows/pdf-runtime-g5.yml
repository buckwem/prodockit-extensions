# Copyright (c) 2026 Mark Buckwell and contributors
# SPDX-License-Identifier: MIT

name: Optional PDF renderers G5 acceptance
on:
  pull_request:
    paths:
      - .github/workflows/pdf-runtime-g5.yml
      - pyproject.toml
      - src/prodockit/pdf/**
      - tools/pdf_runtime_g5_acceptance.py
      - pdk-pdf.toml
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  real-downloads:
    name: ${{ matrix.name }}
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Ubuntu x64
            artifact: ubuntu-x64
            runner: ubuntu-24.04
          - name: Ubuntu ARM64
            artifact: ubuntu-arm64
            runner: ubuntu-24.04-arm
          - name: macOS ARM64
            artifact: macos-arm64
            runner: macos-15
          - name: Windows x64
            artifact: windows-x64
            runner: windows-2025
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version-file: .python-version
          cache: pip
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Install Linux PDF libraries
        if: runner.os == 'Linux'
        run: >-
          sudo apt-get update && sudo apt-get install -y
          libpango-1.0-0 libpangoft2-1.0-0 libharfbuzz-subset0
      - name: Install macOS PDF libraries
        if: runner.os == 'macOS'
        run: |
          brew install pango
          echo "DYLD_FALLBACK_LIBRARY_PATH=$(brew --prefix)/lib" >> "$GITHUB_ENV"
      - name: Install candidate without renderer packages
        run: >-
          python -m pip install -e ".[testing]"
          "zensical==0.0.61" "weasyprint==69.0"
      - name: Exercise real cold downloads, offline warm reuse and PDF rendering
        run: >-
          python tools/pdf_runtime_g5_acceptance.py
          --work-dir pdf-runtime-g5-work
          --report pdf-runtime-g5.json
      - name: Keep acceptance evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pdf-runtime-g5-${{ matrix.artifact }}
          path: |
            pdf-runtime-g5.json
            pdf-runtime-g5-work/project/docs/g5.pdf
          if-no-files-found: warn
